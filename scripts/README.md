# Scripts

This directory is intended for installation and setup scripts.

Before adding a new script, document the following in comments or documentation:

- what the script does;
- which environment variables it requires;
- whether it can be run repeatedly without side effects.
